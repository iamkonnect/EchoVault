$kind: http-request
name: Get All Streams
method: GET
url: '{{baseUrl}}/api/live/streams'
order: 1000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", function () {
        pm.response.to.have.status(200);
      });
